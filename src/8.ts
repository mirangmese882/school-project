// A simple TypeScript function that takes two numbers and returns their sum
function add(a: number, b: number): number {
  return a + b;
}

// Example usage of the above function
const result = add(3, 4); // Result is 7
console.log(result);
